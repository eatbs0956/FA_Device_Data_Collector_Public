# FA_Device_Data_Collector_Public

This repository only hosts **deployment assets** (docker-compose, docs, license).
The actual binary images live in **GHCR** and **GitHub Releases**.

The content of this repository is automatically populated by the release
workflow in the private source repository when a new tag `vX.Y.Z` is pushed.

> First release pending — please check back after the initial `v0.1.0` tag is published.
