# backend service base url, test environment (dev script uses --mode test)
# Gateway.Api handles routing to Auth.Api and Device.Api
VITE_SERVICE_BASE_URL=http://localhost:60620

# other backend service base url, test environment
VITE_OTHER_SERVICE_BASE_URL={}
